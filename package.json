{
  "name": "wrfxweb",
  "private": true,
  "version": "0.0.0",
  "description": "A visualization server for the outputs of fire-weather model wrf-sfire as generated by *wrfxpy*",
  "license": "SJSU",
  "devDependencies": {
    "http-server": "^0.12.3",
    "jasmine-core": "^2.3.4",
    "shelljs": "^0.2.6"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server -g -a localhost -p 8000 -c-1"
  }
}
