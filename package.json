{
  "name": "wrfxweb",
  "private": true,
  "version": "0.0.0",
  "description": "A visualization server for the outputs of fire-weather model wrf-sfire as generated by *wrfxpy*",
  "license": "SJSU",
  "devDependencies": {
    "@babel/cli": "^7.12.10",
    "@babel/core": "^7.12.10",
    "@babel/plugin-transform-runtime": "^7.12.10",
    "@babel/preset-env": "^7.12.11",
    "http-server": "^0.12.3",
    "jasmine-core": "^2.3.4",
    "jest": "^26.6.3",
    "shelljs": "^0.2.6"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server -g -a localhost -p 8000 -c-1 -P http://localhost:5000",
    "start-api": "cd api && venv/bin/flask run --no-debugger",
    "test": "jest --silent"
  }
}
